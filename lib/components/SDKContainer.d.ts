import React from 'react';
import type { ReactNode } from 'react';
type Props = {
    children: ReactNode;
    isDarkTheme: boolean;
};
export declare const SDKContainer: ({ children, isDarkTheme }: Props) => React.JSX.Element;
export {};
